/**
 * MODULE 03A compatibility facade — implementations live in quote-itinerary-sync (04A).
 */
export {
  flightItineraryItem,
  mergeFlightsIntoItinerary,
  stripAutoFlightItems,
  isAutoManagedItineraryDay,
} from "./quote-itinerary-sync";
